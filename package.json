{
  "name": "@candor/core",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "workspaces": [
    "packages/*",
    "examples/*",
    "frontend/",
    "ui-kit/",
    "registry-frontend/",
    "registry-contracts/"
  ],
  "scripts": {
    "watch": "pnpm --parallel --stream --filter=!./frontend --filter=!./registry-frontend -r watch",
    "build": "pnpm --workspace-concurrency=1 --filter=!./frontend --filter=!./registry-frontend -r build",
    "test": "pnpm --parallel --stream -r test"
  },
  "keywords": [],
  "author": "Drew Ewing",
  "license": "ISC"
}
