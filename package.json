{
  "name": "@candor/core",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "index.js",
  "workspaces": [
    "packages/*",
    "examples/*",
    "frontend/",
    "ui-kit/",
    "registry-frontend/",
    "registry-contracts/"
  ],
  "scripts": {
    "watch": "node scripts/build.js --watch --build",
    "build": "node scripts/build.js --build",
    "test": "pnpm --parallel --stream --filter=./packages/* -r test"
  },
  "keywords": [],
  "author": "Drew Ewing",
  "license": "ISC",
  "devDependencies": {
    "chokidar": "^3.5.3",
    "minimist": "^1.2.7"
  }
}
