{
  "name": "@candor/core",
  "private": true,
  "version": "0.1.0",
  "description": "",
  "type": "module",
  "main": "index.js",
  "workspaces": [
    "packages/*",
    "examples/*",
    "frontend/",
    "ui-kit/",
    "registry-frontend/",
    "registry-contracts/"
  ],
  "scripts": {
    "watch": "turbo watch --filter='./packages/*' --filter='./ui-kit' --continue --parallel",
    "build": "turbo build --filter='./packages/*' --filter='./ui-kit' --continue --parallel",
    "dev": "turbo dev",
    "test": "pnpm --parallel --stream --filter=./packages/* -r test"
  },
  "keywords": [],
  "author": "Drew Ewing",
  "license": "ISC",
  "devDependencies": {
    "chokidar": "^3.5.3",
    "minimist": "^1.2.8"
  }
}
