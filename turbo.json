{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "inputs": ["src/**"],
      "outputs": ["dist/**"]
    },
    "watch": {
      "inputs": ["src/**"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "inputs": ["src/**"],
      "outputs": ["dist/**"]
    },
    "lint": {}
  }
}
