{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "inputs": ["src/**", "!ui-kit/", "!frontend", "!registry-frontend"],
      "outputs": ["dist/**"]
    },
    "watch": {
      "inputs": ["src/**", "!ui-kit/", "!frontend", "!registry-frontend"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "inputs": ["src/**"],
      "outputs": ["dist/**"]
    },
    "lint": {}
  }
}
